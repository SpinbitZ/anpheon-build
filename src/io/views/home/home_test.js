"use strict";
module.exports = function (plugin) {
    var test = describe("'home'' should have all its functions", function () {
        //it("Post should ...", function testOn(item) {
        //    assert(false, "home function failed...");
        //});
    });
    return test;
};